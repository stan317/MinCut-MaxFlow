import networkx as nx


# P : original image
# L : labeling of the original image
# V(.,.) : interaction potential
# set_N : set of all pairs of neighboring pixels

def penalty(lp: int) -> float:
    """
    Compute the penalty function evaluated at the label of the image pixel
    :param lp:
    :return:
    :rtype: float
    """

    pass


def V(lp: int, lq: int) -> float:
    """
    Compute the interaction potential between two labels of image pixels
    :param lp:
    :param lq:
    :return:
    :rtype: float
    """

    pass
